# DDR Report Generation System

An AI workflow that reads inspection and thermal report PDFs,
extracts text and images, and generates a structured
Detailed Diagnostic Report (DDR) for clients.

## Setup

```bash
python -m venv ddr_env
ddr_env\Scripts\Activate
pip install -r requirements.txt